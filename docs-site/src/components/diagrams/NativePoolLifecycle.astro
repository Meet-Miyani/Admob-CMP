---
// docs-site/src/components/diagrams/NativePoolLifecycle.astro
//
// PLACEHOLDER. Plan 4 (visual system) replaces this body with the real
// theme-aware diagram. The file name and the zero-prop contract are fixed by
// Plan 3 and must not change — 24 content pages import it as <NativePoolLifecycle />.
//
// Plan 3's final audit (Task 27) fails if this placeholder is still present,
// so a stub can never ship to ads.avinya.dev.
---

<figure class="diagram-placeholder" data-diagram="NativePoolLifecycle">
  <p>Diagram <code>NativePoolLifecycle</code> is not authored yet.</p>
</figure>

<style>
  .diagram-placeholder {
    margin: 1.5rem 0;
    padding: 1rem;
    border: 2px dashed var(--sl-color-gray-4);
    border-radius: 0.5rem;
    text-align: center;
    color: var(--sl-color-gray-3);
  }
</style>
